# Les règles qui protègent le SDK Nothing voyagent avec `core:glyph`
# (consumer-rules.pro) : elles sont là où est la dépendance, pas ici.

# Compose et AndroidX apportent les leurs. Ce fichier n'existe que pour les
# règles propres à l'assemblage — il est vide, et c'est bon signe.
