<?xml version="1.0" encoding="utf-8"?>
<!--
  Fusionné dans la seule variante debug. Le widget de vérification prouve la
  chaîne complète sur un vrai launcher — bitmap, budget binder, taille réelle,
  rafale au tap — sans embarquer une mire dans l'app publiée.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".debug.SelfTestWidget"
            android:exported="false"
            android:label="@string/self_test_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/self_test_widget_info" />
        </receiver>
    </application>

</manifest>
